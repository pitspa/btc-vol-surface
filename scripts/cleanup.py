"""
cleanup.py — FIFO Rolling Window Cleanup

Scans the surfaces/ directory and deletes any CSV files
older than the rolling window defined in config.json.

Usage:
    python scripts/cleanup.py
"""

# Implementation will be built in the coding phase.
# This placeholder establishes the file in the repo structure.

if __name__ == "__main__":
    print("cleanup.py — not yet implemented. Coming in the coding phase.")
